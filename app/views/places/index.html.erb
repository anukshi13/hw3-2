<h1>Places I’ve Been</h1>
<ul>
  <% if @places.present? %>
    <% @places.each do |place| %>
      <li><%= link_to place.name, place_path(place) %></li>
    <% end %>
  <% else %>
    <li>No places yet. <%= link_to "New Place", new_place_path %></li>
  <% end %>
</ul>
<%= link_to "New Place", new_place_path %>